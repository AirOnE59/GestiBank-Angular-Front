export class AffectationDemandeToAgent {

  idDemande: string;
  idAgent: number;

  constructor(idDemande: string, idAgent: number) {
    this.idDemande = idDemande;
    this.idAgent = idAgent;
  }
}
